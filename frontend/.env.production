# Production API base URL for the Veridex frontend (used by Vercel at build time).
# Points at the Flask backend Hugging Face Docker Space.
# Override in the Vercel dashboard (Settings → Environment Variables) if your
# backend Space has a different name.
NEXT_PUBLIC_API_URL=https://krishivvv-veridex-api.hf.space
