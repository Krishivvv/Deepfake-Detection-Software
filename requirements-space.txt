# Dependencies for the Hugging Face Space (CPU inference demo only).
# Kept separate from the full-stack requirements.txt (no Flask/auth/web stack).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2
numpy>=1.24,<2.0
Pillow>=10.0
opencv-python-headless>=4.8
facenet-pytorch>=2.5
scikit-learn>=1.3
PyYAML>=6.0
huggingface_hub>=0.23
gradio>=4.44,<5.0
