# Dependencies for the Hugging Face Space (CPU inference demo only).
# Kept separate from the full-stack requirements.txt (no Flask/auth/web stack).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2
numpy>=1.24,<2.0
Pillow>=10.0
opencv-python-headless>=4.8
facenet-pytorch>=2.5
scikit-learn>=1.3
PyYAML>=6.0
# Pin <1.0: gradio 4.44's oauth.py imports HfFolder, which huggingface_hub 1.0 removed.
huggingface_hub>=0.23,<1.0
# Pin <2.11: pydantic 2.11 emits bool `additionalProperties` in JSON schemas,
# which gradio 4.44's gradio_client can't parse ("argument of type 'bool' is
# not iterable" at launch).
pydantic>=2.0,<2.11
# Pin fastapi<0.113 -> starlette<0.39: gradio 4.44 calls TemplateResponse with
# the old (name, context) signature, which newer starlette rejects
# ("unhashable type: 'dict'" / "localhost is not accessible" at launch).
fastapi>=0.110,<0.113
gradio>=4.44,<5.0
