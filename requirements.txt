# Full-stack app dependencies (backend + ML + video).
# Pinned with compatible upper bounds for reproducibility; Dependabot
# (.github/dependabot.yml) proposes weekly bumps. For the CPU inference demo
# only, use the leaner, fully-pinned requirements-space.txt instead.

# Core ML stack
torch>=2.1,<3.0
torchvision>=0.16,<1.0
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
Pillow>=10.0,<12.0
tqdm>=4.65,<5.0
PyYAML>=6.0,<7.0

# Video / face preprocessing
opencv-python-headless>=4.8,<5.0
facenet-pytorch>=2.5,<3.0

# Web backend
flask>=3.0,<4.0
flask-cors>=4.0,<6.0
flask-sqlalchemy>=3.1,<4.0
flask-bcrypt>=1.0,<2.0
pyjwt>=2.8,<3.0

# Production WSGI server (used when SERVE_WITH_WAITRESS=1)
waitress>=3.0,<4.0
